import { styleWithLayer } from '@/styleUtils';

export const cardInteraction = styleWithLayer({
  position: 'relative',
  overflow: 'hidden',

  height: '100%',

  cursor: 'pointer',
  userSelect: 'none',
});
