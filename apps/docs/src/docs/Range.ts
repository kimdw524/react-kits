import { Range } from '@kimdw-rtk/ui';

import { DocsProps } from '@/plugins/docs-generator';

export { Range };

export default {
  color: 'Range의 color',
  size: 'Range의 size',
} satisfies DocsProps<typeof Range>;
